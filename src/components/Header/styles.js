import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    container: {
        backgroundColor: '#5488c7',
        color: 'white',
        marginBottom: 20,
        fontWeight: 'lighter',
        padding: '5px 0',

    },
}))